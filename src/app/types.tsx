export type ImageContainerProps = {
  profile: boolean
}

export type TypeContainerProps = {
  profile: boolean
}

export type StatBoxProps = {
  profile: boolean
}

export type LiProps = {
  selector: number,
  data: number
}